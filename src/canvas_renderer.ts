class CanvasRenderer {

    constructor() {
    }

    clear() {
    }

    render(canvas: any, virtualWidth: number, virtualHeight: number, viewPort: number[], themeName: string ) {
    }
}

export default CanvasRenderer;
